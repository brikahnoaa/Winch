# .bashrc

# Source global definitions
if [ -f /etc/bashrc ]; then
	. /etc/bashrc
fi

# User specific aliases and functions

export BLOCKSIZE=1G
alias lf='ls -F'

sshh () {
  if [ ! -r "$SSH_AUTH_SOCK" ]; then eval `ssh-agent`; fi
  ssh-add
}
