test larger irid file, 30K

strip quotes from VEE params, irid params
resolve ant & ctd

antDepth blocks, extern antData()

winch up: primary stop method is NGK slack, secondary is antmod depth
    start timer at surf depth detect, watch for stopCmd
        manual stop if timeout (90 sec?)
    log and report time between surf detect and winchStop
    after stop, wait 1 minute and check depth
        restart sequence if now below surf (pushed under)
            repeat cycle up to 5x 

param: pause during 8m current check to allow drift
